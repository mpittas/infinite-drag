// This file is no longer the primary entry point for the UI.
// React initialization is handled by src/main.tsx and src/App.tsx.
// The InfiniteDragCanvas is now managed by src/CanvasDisplay.tsx.

console.log("src/main.ts is loaded, but UI is managed by React (main.tsx).");
